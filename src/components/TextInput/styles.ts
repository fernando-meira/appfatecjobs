import styled from 'styled-components/native';
import { heightPercentageToDP as wh } from 'react-native-responsive-screen';

export const Wrapper = styled.View`
  margin-top: ${wh(1)}px;
  margin-bottom: ${wh(2)}px;
`;
